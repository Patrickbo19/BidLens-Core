{
  "name": "bidlens-core-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic RFP knockout screening, fit scoring, and compliance-matrix MCP/API service.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "mcp": "node src/mcp.js",
    "test": "node --test",
    "check": "node --check src/analysis.js && node --check src/server.js && node --check src/mcp.js"
  },
  "license": "MIT"
}
